"""PyQt6 administration dashboard for the attendance terminal.

The dashboard reuses the existing camera, recognition, tracking, attendance and
persistence workers; it only replaces the OpenCV window with widgets.
"""
